/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.coingecko.com', 'www.coingecko.com', 'www.cryptocompare.com', 'www.cryptocompare.com', 'coin-images.coingecko.com'],
    },
};
 

export default nextConfig;
